# This file is generated by mockapi.
# Do not edit by hand.

openapi: 3.1.0
info:
  title: "Mock API Admin API"
  version: 0.1.0
paths:
  /__mock/health:
    get:
      operationId: mockHealth
      responses:
        '200':
          description: Mock server health
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockHealth'
  /__mock/reset:
    post:
      operationId: mockResetState
      responses:
        '200':
          description: Reset state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockState'
  /__mock/state:
    get:
      operationId: mockGetState
      responses:
        '200':
          description: Current state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockState'
    put:
      operationId: mockPutState
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MockState'
      responses:
        '200':
          description: Replaced state
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockState'
  /__mock/snapshot:
    get:
      operationId: mockGetSnapshot
      responses:
        '200':
          description: Current state snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockState'
    post:
      operationId: mockPostSnapshot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MockState'
      responses:
        '200':
          description: Stored state snapshot
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockState'
  /__mock/clock:
    put:
      operationId: mockPutClock
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MockClock'
      responses:
        '200':
          description: Updated clock
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/MockClock'
components:
  schemas:
    MockHealth:
      type: object
      required: [ok, operationCount]
      properties:
        ok:
          type: boolean
        operationCount:
          type: integer
        serverName:
          type: string
        version:
          type: string
    MockClock:
      type: object
      required: [now]
      properties:
        now:
          type: string
          format: date-time
    MockState:
      type: object
      additionalProperties: false
      required:
        - schemaVersion
        - clock
        - workspaces
        - activeWorkspace
        - folders
        - decks
        - notes
        - reviewSessions
        - settings
        - trash
        - idCounters
      properties:
        schemaVersion:
          type: integer
          enum:
            - 1
        clock:
          $ref: '#/components/schemas/MockClock'
        workspaces:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceRecord"
        activeWorkspace:
          $ref: "#/components/schemas/ActiveWorkspaceRecord"
        folders:
          type: array
          items:
            $ref: "#/components/schemas/FolderRecord"
        decks:
          type: array
          items:
            $ref: "#/components/schemas/DeckRecord"
        notes:
          type: array
          items:
            $ref: "#/components/schemas/NoteDetailRecord"
        reviewSessions:
          type: array
          items:
            $ref: "#/components/schemas/ReviewSessionRecord"
        settings:
          $ref: "#/components/schemas/SettingsRecord"
        trash:
          $ref: "#/components/schemas/TrashStateRecord"
        idCounters:
          type: object
          additionalProperties:
            type: number
    WorkspaceRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/Workspace"
          x-mockapi-source-name: "clear-web-api"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    ActiveWorkspaceRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/ActiveWorkspace"
          x-mockapi-source-name: "clear-web-api"
    FolderRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/Folder"
          x-mockapi-source-name: "clear-web-api"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    DeckRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/Deck"
          x-mockapi-source-name: "clear-web-api"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    NoteDetailRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/NoteDetail"
          x-mockapi-source-name: "clear-web-api"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    ReviewSessionRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/ReviewSession"
          x-mockapi-source-name: "clear-web-api"
        - type: object
          properties:
            id:
              type: string
    SettingsRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/Settings"
          x-mockapi-source-name: "clear-web-api"
    TrashStateRecord:
      allOf:
        - $ref: "../../openapi/openapi.yaml#/components/schemas/TrashState"
          x-mockapi-source-name: "clear-web-api"
