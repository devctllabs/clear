# This file is generated by mockapi.
# Do not edit by hand.

openapi: 3.1.0
info:
  title: Mock API Admin API
  version: 0.1.0
paths:
  /__mock/health:
    get:
      operationId: mockHealth
      responses:
        "200":
          description: Mock server health
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MockHealth"
  /__mock/reset:
    post:
      operationId: mockResetState
      responses:
        "200":
          description: Reset state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MockState"
  /__mock/state:
    get:
      operationId: mockGetState
      responses:
        "200":
          description: Current state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MockState"
    put:
      operationId: mockPutState
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MockState"
      responses:
        "200":
          description: Replaced state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MockState"
  /__mock/snapshot:
    get:
      operationId: mockGetSnapshot
      responses:
        "200":
          description: Current state snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MockState"
    post:
      operationId: mockPostSnapshot
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MockState"
      responses:
        "200":
          description: Stored state snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MockState"
  /__mock/clock:
    put:
      operationId: mockPutClock
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MockClock"
      responses:
        "200":
          description: Updated clock
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MockClock"
components:
  schemas:
    MockHealth:
      type: object
      required:
        - ok
        - operationCount
      properties:
        ok:
          type: boolean
        operationCount:
          type: integer
        serverName:
          type: string
        version:
          type: string
    MockClock:
      type: object
      required:
        - now
      properties:
        now:
          type: string
          format: date-time
    MockState:
      type: object
      additionalProperties: false
      required:
        - schemaVersion
        - clock
        - workspaces
        - activeWorkspace
        - folders
        - decks
        - notes
        - reviewSessions
        - settings
        - trash
        - idCounters
      properties:
        schemaVersion:
          type: integer
          enum:
            - 1
        clock:
          $ref: "#/components/schemas/MockClock"
        workspaces:
          type: array
          items:
            $ref: "#/components/schemas/WorkspaceRecord"
        activeWorkspace:
          $ref: "#/components/schemas/ActiveWorkspaceRecord"
        folders:
          type: array
          items:
            $ref: "#/components/schemas/FolderRecord"
        decks:
          type: array
          items:
            $ref: "#/components/schemas/DeckRecord"
        notes:
          type: array
          items:
            $ref: "#/components/schemas/NoteDetailRecord"
        reviewSessions:
          type: array
          items:
            $ref: "#/components/schemas/ReviewSessionRecord"
        settings:
          $ref: "#/components/schemas/SettingsRecord"
        trash:
          $ref: "#/components/schemas/TrashStateRecord"
        idCounters:
          type: object
          additionalProperties:
            type: number
    WorkspaceRecord:
      allOf:
        - $ref: "#/components/schemas/Workspace"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    ActiveWorkspaceRecord:
      allOf:
        - $ref: "#/components/schemas/ActiveWorkspace"
    FolderRecord:
      allOf:
        - $ref: "#/components/schemas/Folder"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    DeckRecord:
      allOf:
        - $ref: "#/components/schemas/Deck"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    NoteDetailRecord:
      allOf:
        - $ref: "#/components/schemas/NoteDetail"
        - type: object
          properties:
            id:
              type: string
            deletedAt:
              type: string
              format: date-time
              nullable: true
    ReviewSessionRecord:
      allOf:
        - $ref: "#/components/schemas/ReviewSession"
        - type: object
          properties:
            id:
              type: string
    SettingsRecord:
      allOf:
        - $ref: "#/components/schemas/Settings"
    TrashStateRecord:
      allOf:
        - $ref: "#/components/schemas/TrashState"
    Workspace:
      $ref: "#/components/schemas/DomainsWorkspacesWorkspace"
    DomainsWorkspacesWorkspace:
      type: object
      additionalProperties: false
      required:
        - description
        - icon
        - id
        - title
        - updatedAt
      properties:
        description:
          type: string
        icon:
          $ref: "#/components/schemas/VisualIconName"
        id:
          $ref: "#/components/schemas/Id"
        title:
          type: string
          minLength: 1
        updatedAt:
          $ref: "#/components/schemas/DateTime"
      examples:
        - description: Current research, publications, and active study streams.
          icon: layers-3
          id: editorial-production
          title: Editorial Production
          updatedAt: 2026-05-15T12:00:00.000Z
    VisualIconName:
      type: string
      minLength: 1
      description: Lucide icon name used by the UI visual picker.
      example: book-open
    Id:
      type: string
      minLength: 1
      example: editorial-production
    DateTime:
      type: string
      format: date-time
      example: 2026-05-15T12:00:00.000Z
    ActiveWorkspace:
      $ref: "#/components/schemas/DomainsWorkspacesActiveWorkspace"
    DomainsWorkspacesActiveWorkspace:
      type: object
      additionalProperties: false
      required:
        - workspaceId
      properties:
        workspaceId:
          $ref: "#/components/schemas/Id"
    Folder:
      $ref: "#/components/schemas/DomainsFoldersFolder"
    DomainsFoldersFolder:
      type: object
      additionalProperties: false
      required:
        - description
        - id
        - name
        - parentId
        - updatedAt
        - workspaceId
      properties:
        description:
          type: string
        id:
          $ref: "#/components/schemas/Id"
        name:
          type: string
          minLength: 1
        parentId:
          $ref: "#/components/schemas/Id"
        updatedAt:
          $ref: "#/components/schemas/DateTime"
        workspaceId:
          $ref: "#/components/schemas/Id"
      examples:
        - description: Reference materials and study decks.
          id: academic
          name: Academic
          parentId: editorial-production
          updatedAt: 2026-05-15T12:00:00.000Z
          workspaceId: editorial-production
    Deck:
      $ref: "#/components/schemas/DomainsDecksDeck"
    DomainsDecksDeck:
      type: object
      additionalProperties: false
      required:
        - description
        - dueToday
        - icon
        - id
        - parentId
        - progress
        - title
        - totalNotes
        - updatedAt
        - workspaceId
      properties:
        description:
          type: string
        dueToday:
          type: integer
          minimum: 0
        icon:
          $ref: "#/components/schemas/VisualIconName"
        id:
          $ref: "#/components/schemas/Id"
        parentId:
          $ref: "#/components/schemas/Id"
        progress:
          type: number
          minimum: 0
          maximum: 100
        title:
          type: string
          minLength: 1
        totalNotes:
          type: integer
          minimum: 0
        updatedAt:
          $ref: "#/components/schemas/DateTime"
        workspaceId:
          $ref: "#/components/schemas/Id"
      examples:
        - description: ""
          dueToday: 24
          icon: book-open
          id: world-history
          parentId: academic
          progress: 82
          title: World History
          totalNotes: 80
          updatedAt: 2026-05-15T12:00:00.000Z
          workspaceId: editorial-production
    NoteDetail:
      $ref: "#/components/schemas/DomainsNotesNoteDetail"
    DomainsNotesNoteDetail:
      oneOf:
        - $ref: "#/components/schemas/BasicNote"
        - $ref: "#/components/schemas/ClozeNote"
      discriminator:
        propertyName: kind
        mapping:
          basic: "#/components/schemas/BasicNote"
          cloze: "#/components/schemas/ClozeNote"
    BasicNote:
      type: object
      additionalProperties: false
      required:
        - deckId
        - dueAt
        - editor
        - id
        - kind
        - progress
        - reviewedAt
        - status
        - title
        - updatedAt
      properties:
        deckId:
          $ref: "#/components/schemas/Id"
        dueAt:
          $ref: "#/components/schemas/DateTime"
        editor:
          $ref: "#/components/schemas/BasicNoteEditor"
        id:
          $ref: "#/components/schemas/Id"
        kind:
          type: string
          enum:
            - basic
        progress:
          type: number
          minimum: 0
          maximum: 100
        reviewedAt:
          $ref: "#/components/schemas/DateTime"
        status:
          $ref: "#/components/schemas/NoteStatus"
        title:
          type: string
          minLength: 1
        updatedAt:
          $ref: "#/components/schemas/DateTime"
    BasicNoteEditor:
      type: object
      additionalProperties: false
      required:
        - back
        - front
      properties:
        back:
          type: string
        front:
          type: string
    NoteStatus:
      type: string
      enum:
        - in-progress
        - mastered
    ClozeNote:
      type: object
      additionalProperties: false
      required:
        - deckId
        - cards
        - dueAt
        - editor
        - id
        - kind
        - progress
        - reviewedAt
        - status
        - title
        - updatedAt
      properties:
        deckId:
          $ref: "#/components/schemas/Id"
        cards:
          type: array
          items:
            $ref: "#/components/schemas/ClozeNoteCard"
        dueAt:
          $ref: "#/components/schemas/DateTime"
        editor:
          $ref: "#/components/schemas/ClozeNoteEditor"
        id:
          $ref: "#/components/schemas/Id"
        kind:
          type: string
          enum:
            - cloze
        progress:
          type: number
          minimum: 0
          maximum: 100
        reviewedAt:
          $ref: "#/components/schemas/DateTime"
        status:
          $ref: "#/components/schemas/NoteStatus"
        title:
          type: string
          minLength: 1
        updatedAt:
          $ref: "#/components/schemas/DateTime"
    ClozeNoteCard:
      type: object
      additionalProperties: false
      required:
        - clozeId
        - dueAt
        - id
        - progress
        - reviewedAt
        - status
        - title
      properties:
        clozeId:
          type: string
          example: c1
        dueAt:
          $ref: "#/components/schemas/DateTime"
        id:
          $ref: "#/components/schemas/Id"
          description: Opaque review-card lifecycle identifier. It is not derived from
            `clozeId`.
        progress:
          type: number
          minimum: 0
          maximum: 100
        reviewedAt:
          $ref: "#/components/schemas/DateTime"
        status:
          $ref: "#/components/schemas/NoteStatus"
        title:
          type: string
    ClozeNoteEditor:
      type: object
      additionalProperties: false
      required:
        - body
      properties:
        body:
          type: string
    ReviewSession:
      $ref: "#/components/schemas/DomainsReviewReviewSession"
    DomainsReviewReviewSession:
      oneOf:
        - $ref: "#/components/schemas/DueReviewSession"
        - $ref: "#/components/schemas/PracticeReviewSession"
      discriminator:
        propertyName: mode
        mapping:
          due: "#/components/schemas/DueReviewSession"
          practice: "#/components/schemas/PracticeReviewSession"
    DueReviewSession:
      type: object
      additionalProperties: false
      required:
        - currentCard
        - deckId
        - durationSeconds
        - id
        - mode
        - plannedCount
        - reviewedCount
        - startedAt
        - status
      properties:
        completedAt:
          $ref: "#/components/schemas/DateTime"
        currentCard:
          $ref: "#/components/schemas/ReviewCardOrNull"
        deckId:
          $ref: "#/components/schemas/Id"
        durationSeconds:
          type: integer
          minimum: 0
        id:
          $ref: "#/components/schemas/Id"
        mode:
          type: string
          enum:
            - due
        plannedCount:
          type: integer
          minimum: 0
        reviewedCount:
          type: integer
          minimum: 0
        startedAt:
          $ref: "#/components/schemas/DateTime"
        status:
          $ref: "#/components/schemas/DueReviewSessionStatus"
      examples:
        - completedAt: 2026-05-16T12:18:00.000Z
          currentCard: null
          deckId: world-history
          durationSeconds: 1080
          id: world-history-review
          mode: due
          plannedCount: 2
          reviewedCount: 2
          startedAt: 2026-05-16T12:00:00.000Z
          status: completed
    ReviewCardOrNull:
      oneOf:
        - $ref: "#/components/schemas/ReviewCard"
        - type: "null"
    ReviewCard:
      oneOf:
        - $ref: "#/components/schemas/BasicReviewCard"
        - $ref: "#/components/schemas/ClozeReviewCard"
      discriminator:
        propertyName: kind
        mapping:
          basic: "#/components/schemas/BasicReviewCard"
          cloze: "#/components/schemas/ClozeReviewCard"
    BasicReviewCard:
      type: object
      additionalProperties: false
      required:
        - back
        - front
        - id
        - kind
        - progress
      properties:
        back:
          type: string
        front:
          type: string
        id:
          $ref: "#/components/schemas/Id"
        kind:
          type: string
          enum:
            - basic
        progress:
          type: number
          minimum: 0
          maximum: 100
    ClozeReviewCard:
      type: object
      additionalProperties: false
      required:
        - body
        - clozeId
        - id
        - kind
        - progress
      properties:
        body:
          type: string
        clozeId:
          type: string
          example: c1
        id:
          $ref: "#/components/schemas/Id"
        kind:
          type: string
          enum:
            - cloze
        progress:
          type: number
          minimum: 0
          maximum: 100
    DueReviewSessionStatus:
      type: string
      enum:
        - active
        - completed
    PracticeReviewSession:
      type: object
      additionalProperties: false
      required:
        - currentCard
        - deckId
        - durationSeconds
        - id
        - mode
        - reviewedCount
        - startedAt
      properties:
        currentCard:
          $ref: "#/components/schemas/ReviewCard"
        deckId:
          $ref: "#/components/schemas/Id"
        durationSeconds:
          type: integer
          minimum: 0
        id:
          $ref: "#/components/schemas/Id"
        mode:
          type: string
          enum:
            - practice
        reviewedCount:
          type: integer
          minimum: 0
        startedAt:
          $ref: "#/components/schemas/DateTime"
    Settings:
      $ref: "#/components/schemas/DomainsSettingsSettings"
    DomainsSettingsSettings:
      type: object
      additionalProperties: false
      required:
        - dailyNewLimit
        - dailyReviewLimit
        - fsrsParams
        - fsrsRetention
        - language
        - masteryHorizonDays
        - newCardsOrder
        - timezone
      properties:
        dailyNewLimit:
          type: integer
          minimum: 0
        dailyReviewLimit:
          type: integer
          minimum: 0
        fsrsParams:
          type: array
          items:
            type: number
        fsrsRetention:
          type: number
          minimum: 0
          maximum: 100
        language:
          type: string
          minLength: 1
        masteryHorizonDays:
          type: integer
          minimum: 0
        newCardsOrder:
          $ref: "#/components/schemas/SettingsNewCardsOrder"
        timezone:
          type: string
          minLength: 1
      examples:
        - dailyNewLimit: 20
          dailyReviewLimit: 100
          fsrsParams:
            - 0.212
            - 1.2931
          fsrsRetention: 90
          language: en-US
          masteryHorizonDays: 30
          newCardsOrder: before_review
          timezone: auto
    SettingsNewCardsOrder:
      type: string
      enum:
        - after_review
        - before_review
        - mixed
    TrashState:
      $ref: "#/components/schemas/DomainsTrashTrashState"
    DomainsTrashTrashState:
      type: object
      additionalProperties: false
      required:
        - items
        - lastEmptiedAt
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/TrashItem"
        lastEmptiedAt:
          $ref: "#/components/schemas/DateTime"
      examples:
        - items:
            - deletedAt: 2026-05-10T12:00:00.000Z
              id: vocabulary-notes
              kind: note
              locationPath:
                - Editorial Production
                - Civic Theory Review
              title: Vocabulary Notes
          lastEmptiedAt: 2026-05-01T12:00:00.000Z
    TrashItem:
      type: object
      additionalProperties: false
      required:
        - deletedAt
        - id
        - kind
        - locationPath
        - title
      properties:
        deletedAt:
          $ref: "#/components/schemas/DateTime"
        id:
          $ref: "#/components/schemas/Id"
        kind:
          $ref: "#/components/schemas/TrashKind"
        locationPath:
          type: array
          minItems: 1
          items:
            type: string
        title:
          type: string
    TrashKind:
      type: string
      enum:
        - deck
        - folder
        - note
        - workspace

