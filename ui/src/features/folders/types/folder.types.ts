export type Folder = {
  description: string
  id: string
  name: string
  parentId: string
  updatedAt: string
  workspaceId: string
}

export type FolderDraft = {
  description: string
  name: string
  parentId: string
}
